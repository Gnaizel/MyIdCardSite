# Сборка идёт внутри образа, локальный target только раздувает контекст.
target/
.git/
.idea/
*.iml

# Секретам в образе не место.
.env
*.p12
*.jks

# Каталоги Caddy с сертификатами (если вдруг лежат рядом).
caddy_data/
caddy_config/
